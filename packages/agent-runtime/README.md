# Agent runtime

Framework-independent TypeScript types and the browser-owned tool loop. The host application injects model transport and tool execution, so this package contains no Next.js, Supabase, OpenAI, or fantasy-football dependencies.
